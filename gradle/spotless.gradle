spotless {
    java {
        removeUnusedImports()
        googleJavaFormat()
    }
    groovyGradle {
        target '**/*.gradle'
        greclipse()
        indentWithSpaces()
        paddedCell()
    }
    // Freshmark has no dependency on java.scripting module
    // https://github.com/diffplug/spotless/issues/206
    // freshmark { target 'README.md'}
}
